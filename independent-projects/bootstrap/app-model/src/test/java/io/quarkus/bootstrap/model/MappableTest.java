package io.quarkus.bootstrap.model;

import org.junit.jupiter.api.Test;

public class MappableTest {

    @Test
    public void testApplicationModel() {
        //final Map<String, Object> mapped = new ApplicationModelBuilder().build().asMap();

        //assertThat(mapped)
        //        .isEqualTo(Map.of(
        //                BootstrapConstants.MAPPABLE_APP_ARTIFACT, ""));
    }
}
